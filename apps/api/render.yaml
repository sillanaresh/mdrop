services:
  - type: web
    name: mdrop-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
        # Optional comma-separated extra origins.
        # The API already allows localhost and https://mdrop-conv.vercel.app by default.
